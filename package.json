{
    "name": "quordle-discord",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd engine && npm install && npm run build && cd ../client && npm install && npm run build && cd ../server && npm install",
        "start": "cd server && npm start",
        "dev:client": "cd client && npm run dev",
        "dev:server": "cd server && npm run dev"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}